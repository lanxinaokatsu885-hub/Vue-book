# 图书馆值班系统 - 项目问题总结

## 目录

1. [前端交互问题](#一、前端交互问题)
2. [排班功能问题](#二、排班功能问题)
3. [代换班功能问题](#三、代换班功能问题)
4. [链接管理相关问题](#四、链接管理相关问题)
5. [内容展示相关问题](#五、内容展示相关问题)
6. [数据和API问题](#六、数据和API问题)
7. [文件管理问题](#七、文件管理问题)
8. [服务器运行相关问题](#八、服务器运行相关问题)
9. [性能和体验问题](#九、性能和体验问题)
10. [安全和权限问题](#十、安全和权限问题)
11. [功能增强需求](#十一、功能增强需求)
12. [技术改进总结](#十二、技术改进总结)

***

## 一、前端交互问题

### 1.1 人员管理按钮无反应

- **问题描述**：点击人员管理按钮没有任何反应
- **根本原因**：DOMContentLoaded事件重复绑定，initUserManage函数未被正确调用
- **解决方案**：合并事件监听器，确保initUserManage在DOM加载完成后被正确调用
- **修改文件**：`paiban/script.js`

### 1.2 图书查找按钮无法打开链接

- **问题描述**：点击图书查找按钮打不开指定链接
- **根本原因**：事件监听器作用域问题，代码在DOM加载前执行
- **解决方案**：将事件绑定移至window\.onload函数中，确保DOM元素加载完成后再绑定事件
- **修改文件**：`book/script.js`

### 1.3 图片预览功能缺失

- **问题描述**：无法点击查看公告和活动的大图
- **解决方案**：添加图片预览弹窗和预览功能，使用事件委托处理动态加载的图片，实现showImagePreview和hideImagePreview函数
- **修改文件**：`book/script.js`、`book/style.css`

### 1.4 移动端浏览器缓存问题

- **问题描述**：手机端微信能正常显示，但自带浏览器显示旧内容
- **根本原因**：手机自带浏览器对静态资源进行缓存，更新后内容未同步
- **解决方案**：为所有静态资源添加版本号参数（如`style.css?v=1.0.1`）
- **修改文件**：`book/index.html`、`paiban/index.html`

### 1.5 按钮事件重复绑定

- **问题描述**：活动签到和签退按钮的点击事件被多次绑定，导致重复触发
- **解决方案**：删除`window.onload`函数中重复绑定的事件代码
- **修改文件**：`book/script.js`

***

## 二、排班功能问题

### 2.1 排班编辑页404错误

- **问题描述**：访问排班编辑页面时出现404错误
- **根本原因**：Nginx缺少静态文件服务配置，跳转路径使用相对路径
- **解决方案**：添加静态文件服务配置，使用绝对路径跳转，配置location /paiban和location /uploads
- **修改文件**：Nginx配置文件

### 2.2 日期与星期对应错误

- **问题描述**：今天是星期四，但排班表上显示的是星期五
- **根本原因**：星期索引与排班表表头顺序不匹配，日期计算逻辑错误
- **解决方案**：创建星期到索引的映射对象，直接根据当前星期几计算索引，修复getWeekdayIndex函数
- **修改文件**：`book/script.js`

### 2.3 同一人被排到同一时间不同区域

- **问题描述**：同一个人被排到同一时间的两个不同班次，但没有报错
- **根本原因**：checkDuplicateSchedule函数只检查同一个shiftDayKey，没有检查同一班次和日期下的跨区域情况
- **解决方案**：修改checkDuplicateSchedule函数，遍历所有排班记录，检查同一班次+日期的任何区域
- **修改文件**：`paiban/script.js`

### 2.4 当前时间显示和高亮功能不准确

- **问题描述**：时间显示不正确，高亮位置错误
- **解决方案**：添加updateCurrentTime函数实时显示当前时间，定义班次时间段（白一7:30-10:30等），实现highlightCurrentTimeSchedule函数
- **修改文件**：`book/script.js`、`book/style.css`

### 2.5 排班前未设置周数提示

- **问题描述**：用户直接在排班编辑页操作，但没有先设置周数
- **解决方案**：在排班编辑页添加醒目的红色提示，提示用户"请先在左上角设置正确的周数后再进行排班操作"
- **修改文件**：`paiban/index.html`、`paiban/style.css`

### 2.6 周次必填验证缺失

- **问题描述**：排班编辑页未填写周次即可开始编辑，导致数据混乱
- **解决方案**：在排班编辑页面添加周次必填判断逻辑，未填写时阻止编辑并提示
- **修改文件**：`paiban/script.js`

***

## 三、代换班功能问题

### 3.1 代换班记录与排班表周次不同步

- **问题描述**：提交代换班申请后，只在记录中呈现，排班表未更新
- **根本原因**：loadNotices和loadScheduleData并发执行导致竞态条件，数据格式不一致（areas格式vs schedule\_data格式），MySQL UPDATE语法限制
- **解决方案**：重构loadScheduleData确保数据加载完成后调用loadNotices，统一使用areas数据格式，采用INSERT替代UPDATE操作，强化错误处理和状态同步
- **修改文件**：`book/script.js`、`server.js`

### 3.2 代换班记录周次显示问题

- **问题描述**：代换班记录无法按周次正确显示
- **解决方案**：为swap\_requests和substitute\_requests表添加week字段，执行add\_week\_field.sql脚本，修改前端代码按周次过滤和显示记录
- **修改文件**：`add_week_field.sql`、`book/script.js`

### 3.3 代换班操作覆盖其他周次数据（严重BUG）

- **问题描述**：当前是第六周，但排班员提前排了第七周的班，进行代换班操作时会错误覆盖第七周的数据
- **根本原因**：代换班相关API接口总是获取最新的排班数据，而不是根据当前周次获取对应数据
- **解决方案**：修改`/api/swap-shift`、`/api/substitute`、`/api/revoke-shift`三个API接口，使其根据传入的`week`参数获取对应周次的排班数据
- **修改文件**：`server.js`

***

## 四、链接管理相关问题

### 4.1 链接管理表不存在

- **问题描述**：首次运行时数据库中缺少`links`表，导致链接管理功能无法使用
- **解决方案**：在`server.js`中添加自动建表逻辑，服务器启动时自动创建`links`表并插入默认链接数据
- **修改文件**：`server.js`

### 4.2 前端链接数据加载失败

- **问题描述**：网络异常时前端无法获取链接配置
- **解决方案**：在`book/script.js`的`loadLinkData`函数中添加try-catch错误处理，请求失败时使用默认链接数据作为回退
- **修改文件**：`book/script.js`

### 4.3 主页面按钮跳转链接硬编码

- **问题描述**：活动签到、签退、打卡按钮的跳转链接写死在代码中，无法灵活配置
- **解决方案**：创建链接管理系统，从服务器动态加载链接配置，并在排班编辑页新增"链接管理"按钮进行管理
- **修改文件**：`paiban/index.html`、`paiban/script.js`、`book/script.js`、`server.js`

### 4.4 活动签到和签退未设置链接仍跳转

- **问题描述**：未配置链接时点击按钮仍会跳转到空白页面，没有提示信息
- **解决方案**：在按钮点击事件中添加链接非空检查，为空时显示"链接未设置"的提示信息
- **修改文件**：`book/script.js`

***

## 五、内容展示相关问题

### 5.1 公告和活动内容为空时显示空白

- **问题描述**：未设置内容时页面显示空白，没有提示信息
- **解决方案**：修改内容检查逻辑，当`text`为空且`images`为空数组时显示"暂无公告/活动"提示
- **修改文件**：`book/script.js`

### 5.2 负责书架和巡查表在移动端异常

- **问题描述**：负责书架和巡查表弹窗在移动端显示异常，点击图片无法预览
- **解决方案**：在`bindImagePreviewEvents`函数中为负责书架和巡查表弹窗添加图片点击事件监听
- **修改文件**：`book/script.js`

***

## 六、数据和API问题

### 6.1 数据库连接错误

- **错误信息**：`Can't add new command when connection is in closed state`
- **根本原因**：使用单个数据库连接，连接断开后未自动重连
- **解决方案**：将单个连接改为连接池，配置waitForConnections、connectionLimit等参数
- **修改文件**：`server.js`

### 6.2 Mixed Content错误

- **问题描述**：网站使用HTTPS但API请求使用HTTP
- **解决方案**：将所有API请求改为相对路径，使用`/api/xxx`格式
- **修改文件**：`book/script.js`、`paiban/script.js`

### 6.3 数据格式不统一

- **问题描述**：areas格式与schedule\_data格式混用
- **解决方案**：统一使用areas结构存储排班数据，前端和后端约定统一的数据格式
- **修改文件**：`book/script.js`、`paiban/script.js`、`server.js`

### 6.4 数据库连接失败回退

- **问题描述**：数据库用户名或密码错误导致连接失败，系统无法正常运行
- **解决方案**：添加内存存储作为回退方案，数据库操作失败时使用内存变量存储和返回数据
- **修改文件**：`server.js`

***

## 七、文件管理问题

### 7.1 图片删除功能不完整

- **问题描述**：删除图片时只删除前端显示，没有删除服务器文件
- **解决方案**：实现deleteServerImage函数，通过API删除uploads目录中的对应文件
- **修改文件**：`paiban/script.js`、`server.js`

### 7.2 公告和活动图片不显示

- **问题描述**：在排班编辑页上传到公告和活动的图片，在主页面不显示
- **根本原因**：服务器返回的图片URL已包含/uploads/前缀，前端代码重复添加导致URL错误
- **解决方案**：修改loadNoticeData和loadActivityData函数，直接使用服务器返回的图片URL
- **修改文件**：`book/script.js`

### 7.3 favicon.ico 404错误

- **问题描述**：浏览器自动请求favicon.ico但服务器上没有该文件
- **解决方案**：添加favicon引用，指向现有的图标文件
- **修改文件**：`book/index.html`

### 7.4 uploads目录配置问题

- **问题描述**：上传的图片无法访问
- **解决方案**：Nginx配置中添加uploads目录的静态文件服务
- **修改文件**：Nginx配置文件

***

## 八、服务器运行相关问题

### 8.1 服务器启动失败（PowerShell执行策略限制）

- **问题描述**：Windows PowerShell执行策略阻止脚本运行
- **解决方案**：使用`node server.js`命令直接运行，绕过执行策略限制

### 8.2 端口3000被占用

- **问题描述**：端口被其他进程占用导致服务器无法启动
- **解决方案**：使用`taskkill /F /IM node.exe`命令强制停止所有Node.js进程释放端口

***

## 九、性能和体验问题

### 9.1 BGM不是随机节点播放

- **问题描述**：BGM每次都从开头播放，不是随机位置
- **根本原因**：playNext函数参数传递错误，playMusic函数未接收索引参数
- **解决方案**：修改playMusic函数支持索引参数，修复playNext函数正确传递参数，添加随机开始位置（10%-30%的位置）
- **修改文件**：`book/bgm.html`

### 9.2 浏览器缓存导致更新不同步

- **问题描述**：更新代码后，用户看到的是缓存的旧版本
- **解决方案**：为静态资源添加版本号，修改Nginx配置添加缓存控制头
- **修改文件**：`book/index.html`、`paiban/index.html`、Nginx配置文件

### 9.3 移除背景音乐播放功能

- **日期**：2026-06-06
- **问题描述**：用户端和登录页存在背景音乐播放入口，功能必要性不足，影响页面简洁度
- **根本原因**：传统页面通过隐藏 iframe 接入 `book/bgm.html`，Vue 3 首页直接使用 `audio` 标签播放静态音乐文件
- **解决方案**：移除传统主页面和登录页的音乐按钮、隐藏 iframe、消息监听与样式；移除 Vue 3 首页的音乐按钮、`audio` 标签、曲目列表和播放控制函数
- **修改文件**：`book/index.html`、`book/login.html`、`book/script.js`、`book/style.css`、`client/src/views/HomeView.vue`

***

## 十、安全和权限问题

### 10.1 登录认证问题

- **解决方案**：使用express-session管理会话，实现登录、登出功能，密码加密存储
- **修改文件**：`server.js`、`book/login.html`、`book/script.js`

### 10.2 权限控制

- **解决方案**：定义用户角色（normal:普通管理员, scheduler:排班员），不同角色访问不同功能
- **修改文件**：`server.js`、`book/script.js`、`paiban/script.js`

### 10.3 备案号显示

- **问题描述**：需要在页面底部显示备案号
- **解决方案**：在主页面、排班页面和登录页面底部添加备案号，使用红色文字居中显示
- **修改文件**：`book/index.html`、`paiban/index.html`、`book/login.html`

***

## 十一、功能增强需求

### 11.1 新增周次删除功能

- **需求描述**：在排班编辑页的服务器加载弹窗中添加删除周次的功能
- **实现方案**：
  1. 修改周次选择弹窗结构，为每个周次项添加删除按钮
  2. 添加`deleteWeekSchedule`前端函数处理删除请求
  3. 在服务器端添加`/api/delete-schedule`接口
  4. 添加删除按钮样式和交互效果
- **修改文件**：`paiban/index.html`、`paiban/script.js`、`paiban/style.css`、`server.js`

### 11.2 更新网站图标（favicon）

- **需求描述**：更换网站的favicon图标
- **实现方案**：更新`book/index.html`中favicon的引用路径
- **修改文件**：`book/index.html`

### 11.3 负责书架和巡查表改为图片上传模式

- **需求描述**：将主页面巡查表、负责书架功能从硬编码列表改为图片上传模式，在排班编辑页上传图片
- **实现方案**：
  1. 在排班编辑页添加负责书架和巡查表的编辑按钮及弹窗
  2. 实现图片上传和预览功能
  3. 在服务器端添加`/api/shelf`和`/api/inspect`接口
  4. 在主页面添加数据加载和展示逻辑
- **修改文件**：`paiban/index.html`、`paiban/script.js`、`book/script.js`、`server.js`

***

## 十二、技术改进总结

### 12.1 数据持久化

- 使用MySQL存储链接配置、排班数据、代换班记录等，确保数据不丢失
- 服务器启动时自动初始化表结构和默认数据

### 12.2 容错机制

- 实现数据库连接失败时的内存存储回退方案，提高系统可用性
- 添加网络请求失败时的默认数据回退机制

### 12.3 动态配置

- 将硬编码的链接改为从服务器动态加载，支持灵活配置
- 支持在排班编辑页实时管理各种链接配置

### 12.4 用户体验优化

- 添加空状态提示（如"暂无公告/活动"）
- 添加操作确认对话框（如删除周次时的确认）
- 添加统一的错误提示机制（showToast函数）

### 12.5 移动端适配

- 确保弹窗和图片预览功能在移动端正常工作
- 使用响应式设计确保在不同屏幕尺寸下都能正常显示

### 12.6 周次隔离

- 实现不同周次排班数据的隔离处理，避免相互影响
- 代换班操作只影响当前周次的数据

### 12.7 缓存管理

- 为静态资源添加版本号，确保更新能及时生效
- 配置合适的缓存策略，平衡性能和更新频率

***

## 附录：问题分类统计

| 问题类别     | 问题数量   | 严重程度      |
| :------- | :----- | :-------- |
| 前端交互问题   | 5      | 中等        |
| 排班功能问题   | 6      | 高（其中1个严重） |
| 代换班功能问题  | 3      | 高（其中1个严重） |
| 链接管理相关问题 | 4      | 中等        |
| 内容展示相关问题 | 2      | 低         |
| 数据和API问题 | 4      | 高         |
| 文件管理问题   | 4      | 中等        |
| 服务器运行问题  | 2      | 高         |
| 性能和体验问题  | 2      | 中等        |
| 安全和权限问题  | 3      | 高         |
| 功能增强需求   | 3      | 中等        |
| **总计**   | **38** | -         |

***

**文档版本**：v2.6\
**创建日期**：2026年5月17日\
**适用项目**：图书馆值班系统
